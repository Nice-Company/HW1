name: Lint with pylint

on:
  workflow_run:
    workflows: ["Lint with autopep8"]
    types:
      - completed

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python3 -B --version
        python -m pip install --upgrade pip
        pip install --upgrade pylint
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Create post traces directory
      run: mkdir -p post_traces

    - name: Lint with pylint
      run: |
        pylint $(git ls-files '*.py') > post_traces/pylint_trace.txt # running for all the files in the repo
        git config --local user.email "action@github.com"
        git config --local user.name "github-actions"
        git add .
        git commit -m "Saving pylint trace"
        git push origin HEAD:main
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
